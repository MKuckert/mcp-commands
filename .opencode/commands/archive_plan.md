---
description: Archives the current PLAN.md to the plans directory with a descriptive name.
agent: Buddy
---

Archive the current `PLAN.md` file to the `plans` directory. 
The filename should be in the format `plans/YYYY-MM-DD-<specific-postfix>.md` based on today's date (`$TODAY`).

1. Read the current `PLAN.md` to understand what the plan is about.
2. Come up with a short, specific postfix for the plan based on its contents (e.g. `initial`, `add-auth`, `refactor-db`).
3. Ensure the `plans` directory exists.
4. Move or copy the current `PLAN.md` to the new path in the `plans` directory.
5. Create a new empty `PLAN.md` if the previous one was moved.

Let me know what filename you used when you are done.

$ARGUMENTS
